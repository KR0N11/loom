# Data dictionary

Generated by `scripts/write_data_dictionary.py` from `data/semantic/*.json`.

## Dataset `nfip`

### Table `nfip_claims`

NFIP flood insurance claims (redacted) for FL, TX, LA, NJ, NY, NC, SC with year of loss 2015 or later. Amounts paid, coverage, damage estimates, flood zone and building attributes per claim. A bounded slice of the full 2.6M-row dataset. Time range: 2015-01-03 to 2026-05-28 (`date_of_loss`).

- Grain: one row per claim (one policy, one loss date)
- Rows: 150,000

| Column | Type | Description | Unit | Null % | Distinct |
|---|---|---|---|---:|---:|
| `id` | VARCHAR | Unique claim identifier (UUID). |  | 0.0 | 150000 |
| `date_of_loss` | DATE | Date the flood loss occurred. |  | 0.0 | 3244 |
| `year_of_loss` | BIGINT | Calendar year of date_of_loss. | year | 0.0 | 12 |
| `state` | VARCHAR | Two-letter state code of the insured property; join to states.state. |  | 0.0 | 7 |
| `county_code` | VARCHAR | 5-digit FIPS county code. |  | 0.1 | 505 |
| `reported_zip_code` | VARCHAR | ZIP code of the insured property. |  | 0.0 | 3970 |
| `nfip_community_number_current` | VARCHAR | Current NFIP community id; join to communities.community_id_number. |  | 14.0 | 2326 |
| `rated_flood_zone` | VARCHAR | Flood zone used for rating. Zones starting with A or V are Special Flood Hazard Areas (SFHA); X/B/C are lower risk. |  | 0.5 | 54 |
| `flood_zone_current` | VARCHAR | Flood zone on the current effective map. |  | 14.2 | 51 |
| `occupancy_type` | BIGINT | Occupancy code: 1 single-family, 2 two-to-four family, 3 other residential, 4 non-residential, 6 other residential building, 11-18 condo/RCBAP types. |  | 0.0 | 14 |
| `original_construction_date` | DATE | Date the building was originally built. |  | 0.3 | 13595 |
| `elevated_building_indicator` | BOOLEAN | True if the building is elevated. |  | 0.0 | 2 |
| `primary_residence_indicator` | BOOLEAN | True if the insured's primary residence. |  | 0.0 | 2 |
| `post_firm_construction_indicator` | BOOLEAN | True if built after the community's first Flood Insurance Rate Map. |  | 0.0 | 2 |
| `amount_paid_on_building_claim` | DOUBLE | Dollars paid for building damage. | USD | 14.1 | 106940 |
| `amount_paid_on_contents_claim` | DOUBLE | Dollars paid for contents damage. | USD | 14.1 | 43939 |
| `amount_paid_on_increased_cost_of_compliance_claim` | DOUBLE | Dollars paid under Increased Cost of Compliance (ICC) coverage. | USD | 14.1 | 351 |
| `total_building_insurance_coverage` | DOUBLE | Building coverage limit. | USD | 0.0 | 3963 |
| `total_contents_insurance_coverage` | DOUBLE | Contents coverage limit. | USD | 0.0 | 1330 |
| `building_damage_amount` | DOUBLE | Adjuster's estimate of building damage. | USD | 14.7 | 78769 |
| `contents_damage_amount` | DOUBLE | Adjuster's estimate of contents damage. | USD | 47.1 | 38498 |
| `net_building_payment_amount` | DOUBLE | Net building payment after adjustments. | USD | 0.0 | 106951 |
| `net_contents_payment_amount` | DOUBLE | Net contents payment after adjustments. | USD | 0.0 | 43944 |
| `cause_of_damage` | VARCHAR | Cause code as text: 0 other, 1 tidal water overflow, 2 stream/river overflow, 3 alluvial fan, 4 accumulation of rainfall/snowmelt, 7 erosion, 8 mudflow, 9 earth movement. |  | 1.5 | 11 |
| `water_depth` | BIGINT | Depth of flood water in the building. | feet | 11.3 | 229 |
| `number_of_floors_in_the_insured_building` | BIGINT | Number of floors. | floors | 0.6 | 6 |
| `policy_count` | BIGINT | Number of policies/units on the claim (usually 1). | policies | 0.0 | 227 |
| `rate_method` | VARCHAR | Rating method code. |  | 0.0 | 17 |
| `flood_event` | VARCHAR | Named flood event, e.g. Hurricane Ian; null for unnamed events. |  | 12.1 | 61 |
| `total_paid` | DOUBLE | Derived: building + contents + ICC amounts paid (nulls treated as 0). | USD | 0.0 | 111618 |
| `is_sfha` | BOOLEAN | Derived: rated_flood_zone starts with A or V (Special Flood Hazard Area). |  | 0.0 | 2 |

### Table `nfip_communities`

NFIP Community Status Book: every community in the slice states with its NFIP participation status and initial flood-map dates.

- Grain: one row per NFIP community
- Rows: 5,200

| Column | Type | Description | Unit | Null % | Distinct |
|---|---|---|---|---:|---:|
| `community_id_number` | VARCHAR | 6-character NFIP community id; join key to claims. |  | 0.0 | 5200 |
| `community_name` | VARCHAR | Community name as listed by FEMA. |  | 0.0 | 4970 |
| `county` | VARCHAR | County name. |  | 0.0 | 709 |
| `state` | VARCHAR | Two-letter state code. |  | 0.0 | 7 |
| `initial_flood_hazard_boundary_map` | DATE | Date of the first flood hazard boundary map. |  | 17.1 | 534 |
| `initial_flood_insurance_rate_map` | DATE | Date of the first Flood Insurance Rate Map (FIRM). |  | 3.9 | 1025 |
| `participating_in_nfip` | BOOLEAN | True if the community currently participates in NFIP. |  | 0.0 | 2 |
| `tribal` | BOOLEAN | True for tribal communities. |  | 0.0 | 2 |
| `class_rating` | BIGINT | Community Rating System class (1 best, 10 none); null if not rated. |  | 86.4 | 9 |
| `sanction` | BOOLEAN | True if the community is sanctioned / suspended. |  | 0.0 | 2 |

### Table `nfip_states`

States and territories with their full name and FEMA region number.

- Grain: one row per state
- Rows: 56

| Column | Type | Description | Unit | Null % | Distinct |
|---|---|---|---|---:|---:|
| `state` | VARCHAR | Two-letter state or territory code. |  | 0.0 | 56 |
| `state_name` | VARCHAR | Full state name. |  | 0.0 | 56 |
| `fema_region` | BIGINT | FEMA region number (1-10). |  | 0.0 | 10 |

### Allowed joins

| Left | Right | On | Kind | Note |
|---|---|---|---|---|
| `nfip_claims` | `nfip_states` | `nfip_claims.state = nfip_states.state` | left | Every claim state exists in states; use for state names and FEMA regions. |
| `nfip_claims` | `nfip_communities` | `nfip_claims.nfip_community_number_current = nfip_communities.community_id_number` | left | LEFT JOIN: some claims have a null or retired community number. |

### Metrics

| Metric | Definition | SQL | Unit | Grain | Caveats |
|---|---|---|---|---|---|
| `claim_count` | Number of claims. | `COUNT(*)` | claims | any |  |
| `total_paid` | Total dollars paid: building + contents + ICC. | `SUM(total_paid)` | USD | any | Closed-without-payment claims contribute 0, not null. |
| `avg_claim_severity` | Mean total_paid per claim including zero-paid claims. | `AVG(total_paid)` | USD per claim | any | Use AVG(CASE WHEN total_paid > 0 THEN total_paid END) for paid claims only. |
| `paid_claim_share` | Share of claims with any payment. | `AVG(CASE WHEN total_paid > 0 THEN 1.0 ELSE 0.0 END)` | ratio (0-1) | any |  |
| `claim_frequency_per_policy` | Claims per policy on the claim records. | `COUNT(*) * 1.0 / SUM(policy_count)` | claims per policy | any | policy_count is per-claim, not the exposure base; this is not a true frequency. True frequency needs the policies dataset, which is not loaded. |
| `building_loss_ratio` | Building dollars paid divided by building coverage limit. | `SUM(amount_paid_on_building_claim) / NULLIF(SUM(total_building_insurance_coverage), 0)` | ratio | any | Coverage is per claim, not per policy-year, so this overstates a true loss ratio. |
| `sfha_claim_share` | Share of claims in Special Flood Hazard Areas (rated_flood_zone starting with A or V). | `AVG(CASE WHEN is_sfha THEN 1.0 ELSE 0.0 END)` | ratio (0-1) | any |  |
| `median_water_depth` | Median reported flood water depth. | `MEDIAN(water_depth)` | feet | any | water_depth is null for many claims; the median ignores nulls. |
| `avg_building_damage` | Mean adjuster estimate of building damage. | `AVG(building_damage_amount)` | USD | any |  |
| `payment_to_damage_ratio` | Building dollars paid divided by estimated building damage. | `SUM(amount_paid_on_building_claim) / NULLIF(SUM(building_damage_amount), 0)` | ratio | any |  |
| `claims_per_community` | Claims divided by distinct participating communities in scope. | `COUNT(*) * 1.0 / COUNT(DISTINCT nfip_community_number_current)` | claims per community | any |  |

## Dataset `ttc`

### Table `ttc_subway_delays`

Subway and SRT delay incidents logged by TTC control, 2023-2024. Each incident has a station, a delay code (see subway_delay_codes), the delay in minutes and the resulting gap between trains. Time range: 2023-01-01 to 2024-12-31 (`delay_date`).

- Grain: one row per delay incident
- Rows: 49,393

| Column | Type | Description | Unit | Null % | Distinct |
|---|---|---|---|---:|---:|
| `delay_date` | DATE | Calendar date of the incident. |  | 0.0 | 731 |
| `delay_time` | VARCHAR | Local clock time of the incident as HH:MM text (24h). |  | 0.0 | 1437 |
| `day_of_week` | VARCHAR | Weekday name, e.g. Monday. |  | 0.0 | 7 |
| `station` | VARCHAR | Station or location name, upper case, e.g. BLOOR STATION. |  | 0.0 | 677 |
| `code` | VARCHAR | Delay code; join to subway_delay_codes.code for the description. |  | 0.0 | 180 |
| `min_delay` | BIGINT | Length of the delay to the vehicle in minutes. Zero means logged but no delay. | minutes | 0.0 | 132 |
| `min_gap` | BIGINT | Resulting headway gap to the next vehicle in minutes. | minutes | 0.0 | 142 |
| `bound` | VARCHAR | Direction of travel: N, S, E, W (null when not applicable). |  | 36.5 | 5 |
| `line` | VARCHAR | Subway line code: YU (Line 1), BD (Line 2), SHP (Line 4), SRT. |  | 0.2 | 26 |
| `vehicle` | BIGINT | Vehicle number; 0 means unknown. |  | 0.0 | 914 |
| `delay_hour` | BIGINT | Hour of day (0-23) parsed from delay_time. | hour | 0.0 | 24 |
| `mode` | VARCHAR | Transit mode: subway, bus or streetcar. |  | 0.0 | 1 |

### Table `ttc_bus_delays`

Bus delay incidents by route and location, 2023-2024. Causes are free-text incident categories (e.g. Mechanical, Operations - Operator, Collision - TTC). Time range: 2023-01-01 to 2024-12-31 (`delay_date`).

- Grain: one row per delay incident
- Rows: 115,452

| Column | Type | Description | Unit | Null % | Distinct |
|---|---|---|---|---:|---:|
| `delay_date` | DATE | Calendar date of the incident. |  | 0.0 | 731 |
| `route` | VARCHAR | Route number as text, e.g. 501 (streetcar) or 36 (bus). |  | 1.0 | 310 |
| `delay_time` | VARCHAR | Local clock time of the incident as HH:MM text (24h). |  | 0.0 | 1440 |
| `day_of_week` | VARCHAR | Weekday name, e.g. Monday. |  | 0.0 | 7 |
| `location` | VARCHAR | Intersection or stop name where the delay occurred. |  | 0.0 | 17497 |
| `incident` | VARCHAR | Incident category, e.g. Mechanical, Diversion, Collision - TTC. |  | 0.0 | 13 |
| `min_delay` | BIGINT | Length of the delay to the vehicle in minutes. Zero means logged but no delay. | minutes | 0.0 | 660 |
| `min_gap` | BIGINT | Resulting headway gap to the next vehicle in minutes. | minutes | 0.0 | 714 |
| `bound` | VARCHAR | Direction of travel: N, S, E, W (null when not applicable). |  | 16.5 | 19 |
| `vehicle` | BIGINT | Vehicle number; 0 means unknown. |  | 0.0 | 2747 |
| `delay_hour` | BIGINT | Hour of day (0-23) parsed from delay_time. | hour | 0.0 | 24 |
| `mode` | VARCHAR | Transit mode: subway, bus or streetcar. |  | 0.0 | 1 |

### Table `ttc_streetcar_delays`

Streetcar delay incidents by route and location, 2023-2024. Same incident categories as bus delays. Time range: 2023-01-01 to 2024-12-31 (`delay_date`).

- Grain: one row per delay incident
- Rows: 28,582

| Column | Type | Description | Unit | Null % | Distinct |
|---|---|---|---|---:|---:|
| `delay_date` | DATE | Calendar date of the incident. |  | 0.0 | 731 |
| `route` | VARCHAR | Route number as text, e.g. 501 (streetcar) or 36 (bus). |  | 1.0 | 55 |
| `delay_time` | VARCHAR | Local clock time of the incident as HH:MM text (24h). |  | 0.0 | 1440 |
| `day_of_week` | VARCHAR | Weekday name, e.g. Monday. |  | 0.0 | 7 |
| `location` | VARCHAR | Intersection or stop name where the delay occurred. |  | 0.0 | 3765 |
| `incident` | VARCHAR | Incident category, e.g. Mechanical, Diversion, Collision - TTC. |  | 0.0 | 14 |
| `min_delay` | BIGINT | Length of the delay to the vehicle in minutes. Zero means logged but no delay. | minutes | 0.0 | 289 |
| `min_gap` | BIGINT | Resulting headway gap to the next vehicle in minutes. | minutes | 0.0 | 310 |
| `bound` | VARCHAR | Direction of travel: N, S, E, W (null when not applicable). |  | 15.3 | 8 |
| `vehicle` | BIGINT | Vehicle number; 0 means unknown. |  | 0.0 | 1748 |
| `delay_hour` | BIGINT | Hour of day (0-23) parsed from delay_time. | hour | 0.0 | 24 |
| `mode` | VARCHAR | Transit mode: subway, bus or streetcar. |  | 0.0 | 1 |

### Table `ttc_subway_delay_codes`

Lookup of subway delay codes to plain-English descriptions (e.g. MUPAA = Passenger Assistance Alarm Activated).

- Grain: one row per delay code
- Rows: 140

| Column | Type | Description | Unit | Null % | Distinct |
|---|---|---|---|---:|---:|
| `code` | VARCHAR | Delay code, matches subway_delays.code. |  | 0.0 | 140 |
| `description` | VARCHAR | Plain-English description of the code. |  | 0.0 | 140 |

### Allowed joins

| Left | Right | On | Kind | Note |
|---|---|---|---|---|
| `ttc_subway_delays` | `ttc_subway_delay_codes` | `ttc_subway_delays.code = ttc_subway_delay_codes.code` | left | Use LEFT JOIN: some codes in the incident log are missing from the lookup. |
| `ttc_bus_delays` | `ttc_streetcar_delays` | `UNION ALL on shared columns (delay_date, route, min_delay, min_gap, incident, mode)` | union | Bus and streetcar share a schema; combine with UNION ALL, not a join. |

### Metrics

| Metric | Definition | SQL | Unit | Grain | Caveats |
|---|---|---|---|---|---|
| `delay_count` | Number of logged delay incidents. | `COUNT(*)` | incidents | any (filter by mode/line/route/date as needed) | Includes rows with min_delay = 0 (logged, no measurable delay). |
| `total_delay_minutes` | Sum of min_delay across incidents. | `SUM(min_delay)` | minutes | any |  |
| `avg_delay_minutes` | Mean min_delay per incident, including zero-minute rows. | `AVG(min_delay)` | minutes | any | Zero-minute incidents pull the average down; filter min_delay > 0 for 'average of real delays'. |
| `avg_nonzero_delay_minutes` | Mean min_delay over incidents with a positive delay. | `AVG(CASE WHEN min_delay > 0 THEN min_delay END)` | minutes | any |  |
| `delays_per_day` | Incidents divided by distinct days in the filter window. | `COUNT(*) * 1.0 / COUNT(DISTINCT delay_date)` | incidents per day | any | Counts only days with at least one incident. |
| `avg_gap_minutes` | Mean headway gap caused by delays. | `AVG(min_gap)` | minutes | any |  |
| `major_delay_count` | Incidents with a delay of 15 minutes or more. | `SUM(CASE WHEN min_delay >= 15 THEN 1 ELSE 0 END)` | incidents | any |  |
| `major_delay_share` | Share of incidents with min_delay >= 15. | `AVG(CASE WHEN min_delay >= 15 THEN 1.0 ELSE 0.0 END)` | ratio (0-1) | any |  |
| `top_delay_code_share` | Share of subway incidents attributed to the single most common code. | `MAX(code_count) * 1.0 / SUM(code_count) over grouped-by-code counts` | ratio (0-1) | subway only; group by code first | Requires a two-step query: count per code, then compare max to total. |
| `incident_category_minutes` | Total delay minutes by incident category for bus/streetcar. | `SUM(min_delay) GROUP BY incident` | minutes | bus and streetcar only |  |
